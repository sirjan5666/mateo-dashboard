node_modules/
dist/
.env
.env.local
*.log
.DS_Store

# Locally-stored skin-tracker photos (children's health data — never commit)
server/uploads/

# Claude Code local tooling — third-party skill bundles (mixed/unlicensed) and
# local settings; keep out of the commercial repo (see the design-skills review).
.claude/
