# Force LF on files that run on the Linux server — a stray CRLF breaks bash /
# systemd / Caddy with cryptic errors. Everything else: let git normalize.
* text=auto
*.sh text eol=lf
*.service text eol=lf
deploy/Caddyfile text eol=lf
